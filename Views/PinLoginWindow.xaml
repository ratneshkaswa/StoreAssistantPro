<Window x:Class="StoreAssistantPro.Views.PinLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="250" Width="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Enter PIN" FontSize="12" Margin="0,0,0,3"/>
            <TextBox Text="{Binding Pin, UpdateSourceTrigger=PropertyChanged}"
                     Padding="6" MaxLength="4" FontSize="18"
                     HorizontalContentAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red"
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <Button Grid.Row="3" Content="Login" Command="{Binding LoginCommand}"
                Padding="12,8" FontSize="14" FontWeight="SemiBold"
                HorizontalAlignment="Stretch"/>
    </Grid>
</Window>
