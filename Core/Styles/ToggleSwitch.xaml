<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Fluent Toggle Switch — 44x22 pill track with 16px sliding thumb -->
    <Style x:Key="FluentToggleSwitchStyle" TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Width="40"
                          Height="20"
                          UseLayoutRounding="True"
                          SnapsToDevicePixels="True">
                        <Border x:Name="FocusRing"
                                Margin="-3"
                                CornerRadius="12"
                                BorderBrush="{StaticResource FluentAccentDefault}"
                                BorderThickness="0"
                                Opacity="0"
                                SnapsToDevicePixels="True"/>
                        <Border x:Name="Track"
                                CornerRadius="10"
                                Background="{StaticResource FluentControlFillTertiary}"
                                BorderBrush="{StaticResource FluentStrokeDefault}"
                                BorderThickness="1"
                                SnapsToDevicePixels="True"/>
                        <Border x:Name="Thumb"
                                Width="14"
                                Height="14"
                                CornerRadius="7"
                                Background="{StaticResource FluentTextSecondary}"
                                HorizontalAlignment="Left"
                                Margin="3,0,0,0"
                                VerticalAlignment="Center"
                                SnapsToDevicePixels="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="{StaticResource FluentAccentDefault}"/>
                            <Setter TargetName="Track" Property="BorderBrush" Value="{StaticResource FluentAccentDefault}"/>
                            <Setter TargetName="Thumb" Property="Background" Value="{StaticResource FluentTextOnAccent}"/>
                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Track" Property="BorderBrush" Value="{StaticResource FluentStrokeFocus}"/>
                            <Setter TargetName="Thumb" Property="Background" Value="{StaticResource FluentTextPrimary}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="FocusRing" Property="BorderThickness" Value="2"/>
                            <Setter TargetName="FocusRing" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Track" Property="Opacity" Value="0.4"/>
                            <Setter TargetName="Thumb" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Read-only display toggle (setup wizards, info-only grids) -->
    <DataTemplate x:Key="ActiveToggleReadOnlyTemplate">
        <CheckBox Style="{StaticResource FluentToggleSwitchStyle}"
                  IsChecked="{Binding IsActive, Mode=OneWay}"
                  IsHitTestVisible="False"/>
    </DataTemplate>

    <!-- Interactive toggle — fires ToggleActiveCommand on the DataGrid's DataContext.
         Clicking the toggle selects the row (mouse-down) then fires the command (click).
         Binding stays OneWay so the entity is never mutated locally; the command
         round-trips through the service and reloads the list. -->
    <DataTemplate x:Key="ActiveToggleCellTemplate">
        <CheckBox Style="{StaticResource FluentToggleSwitchStyle}"
                  IsChecked="{Binding IsActive, Mode=OneWay}"
                  CommandParameter="{Binding}"
                  Command="{Binding DataContext.ToggleActiveCommand,
                            RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
    </DataTemplate>

</ResourceDictionary>
