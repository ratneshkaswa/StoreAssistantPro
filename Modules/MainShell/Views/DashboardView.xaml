<UserControl x:Class="StoreAssistantPro.Modules.MainShell.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="OnLoaded">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0">
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="{StaticResource SectionSpacing}">
            <TextBlock Text="Dashboard" Style="{StaticResource PageTitleStyle}" Margin="0"
                       DockPanel.Dock="Left"/>
            <Button Content="🔄 Refresh" Command="{Binding LoadDashboardCommand}"
                    HorizontalAlignment="Right" Style="{StaticResource PrimaryButtonStyle}"/>
        </DockPanel>

        <!-- Loading Indicator -->
        <TextBlock Grid.Row="1" Text="⏳ Loading..." FontSize="14" Foreground="#888"
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>

        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}"
                   Style="{StaticResource ErrorMessageStyle}"
                   VerticalAlignment="Top" Margin="0,20,0,0"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <UniformGrid Rows="1" Columns="4" VerticalAlignment="Top">
            <Border Background="#4CAF50" CornerRadius="8" Margin="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Products" Foreground="White" FontSize="14"/>
                    <TextBlock Text="{Binding TotalProducts}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="#FF9800" CornerRadius="8" Margin="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="Low Stock Items" Foreground="White" FontSize="14"/>
                    <TextBlock Text="{Binding LowStockCount}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="#2196F3" CornerRadius="8" Margin="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="Today's Sales" Foreground="White" FontSize="14"/>
                    <TextBlock Text="{Binding TodaysSales, StringFormat=C}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="#9C27B0" CornerRadius="8" Margin="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="Transactions" Foreground="White" FontSize="14"/>
                    <TextBlock Text="{Binding TodaysTransactions}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            </UniformGrid>
        </ScrollViewer>
    </Grid>
    </ScrollViewer>
</UserControl>
