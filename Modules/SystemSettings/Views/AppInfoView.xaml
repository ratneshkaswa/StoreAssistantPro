<UserControl x:Class="StoreAssistantPro.Modules.SystemSettings.Views.AppInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Loaded="OnLoaded">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="ℹ Application Info"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,25"/>

        <!-- App Version -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Application Version" FontSize="12" FontWeight="SemiBold"
                       Foreground="#888" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding AppVersion}" FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Current User -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Current User" FontSize="12" FontWeight="SemiBold"
                       Foreground="#888" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding CurrentUser}" FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Firm Name -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="Firm Name" FontSize="12" FontWeight="SemiBold"
                       Foreground="#888" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding FirmName}" FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Database Connection -->
        <StackPanel Grid.Row="4" Margin="0,0,0,16">
            <TextBlock Text="Database Connection" FontSize="12" FontWeight="SemiBold"
                       Foreground="#888" Margin="0,0,0,2"/>
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#999"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDatabaseConnected}" Value="True">
                                    <Setter Property="Fill" Value="#4CAF50"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsDatabaseConnected}" Value="False">
                                    <Setter Property="Fill" Value="#F44336"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding DatabaseStatus}" FontSize="16" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <Button Content="🔄 Refresh Connection" Command="{Binding CheckConnectionCommand}"
                    Padding="12,6" FontSize="12" Margin="0,8,0,0"
                    HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</UserControl>
