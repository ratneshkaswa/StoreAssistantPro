<UserControl x:Class="StoreAssistantPro.Modules.SystemSettings.Views.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Loaded="OnLoaded">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="🏪 General Settings"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Firm Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="Firm Name *" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox Text="{Binding FirmName, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8" FontSize="13" MaxLength="200" MaxWidth="400"
                     HorizontalAlignment="Left" Width="400"/>
        </StackPanel>

        <!-- Default Currency -->
        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <TextBlock Text="Default Currency" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox ItemsSource="{Binding AvailableCurrencies}"
                      SelectedItem="{Binding DefaultCurrency}"
                      Padding="8" FontSize="13" MaxWidth="200"
                      HorizontalAlignment="Left" Width="200"/>
        </StackPanel>

        <!-- Messages -->
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SuccessMessage}" Foreground="#4CAF50" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Save Button -->
        <StackPanel Grid.Row="5" Orientation="Horizontal">
            <Button Content="💾 Save Settings" Command="{Binding SaveCommand}"
                    Padding="20,10" FontSize="13" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</UserControl>
