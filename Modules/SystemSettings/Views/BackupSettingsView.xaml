<UserControl x:Class="StoreAssistantPro.Modules.SystemSettings.Views.BackupSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Loaded="OnLoaded">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="💾 Backup &amp; Restore"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Backup Folder Display -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Backup Folder" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox Text="{Binding BackupFolder, Mode=OneWay}" IsReadOnly="True"
                     Padding="8" FontSize="12" Foreground="#555" Background="#F0F0F0"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="📤 Backup Database" Command="{Binding BackupDatabaseCommand}"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"
                    Padding="20,10" FontSize="13" FontWeight="SemiBold" Margin="0,0,12,0"/>
            <Button Content="📥 Restore Database" Command="{Binding RestoreDatabaseCommand}"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"
                    Padding="20,10" FontSize="13" FontWeight="SemiBold" Margin="0,0,12,0"/>
            <Button Content="📂 Open Backup Folder" Command="{Binding OpenBackupFolderCommand}"
                    Padding="20,10" FontSize="13"/>
        </StackPanel>

        <!-- Messages -->
        <StackPanel Grid.Row="3">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SuccessMessage}" Foreground="#4CAF50" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
