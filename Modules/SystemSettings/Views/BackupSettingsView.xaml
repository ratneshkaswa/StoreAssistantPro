<UserControl x:Class="StoreAssistantPro.Modules.SystemSettings.Views.BackupSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:h="clr-namespace:StoreAssistantPro.Core.Helpers"
             mc:Ignorable="d"
             Loaded="OnLoaded">
    <Grid Margin="{StaticResource DialogPadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="💾 Backup &amp; Restore" Style="{StaticResource DialogTitleStyle}"/>

        <!-- Two-column form grid -->
        <Grid Grid.Row="1" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="100"/>
                <ColumnDefinition Width="*" MaxWidth="500"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Backup Folder"
                       Style="{StaticResource FormRowLabelStyle}"
                       VerticalAlignment="Top" Margin="0,6,12,0"/>
            <TextBox Grid.Row="0" Grid.Column="1"
                     Text="{Binding BackupFolder, Mode=OneWay}" IsReadOnly="True"
                     Background="{StaticResource FluentSurfaceElevated}"
                     Foreground="{StaticResource FluentTextSecondary}"/>
        </Grid>

        <!-- Action buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    Margin="{StaticResource FormActionBarSpacing}">
            <Button Content="📤 Backup Database" Command="{Binding BackupDatabaseCommand}"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="{StaticResource InlineControlSpacing}"/>
            <Button Content="📥 Restore Database" Command="{Binding RestoreDatabaseCommand}"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="{StaticResource InlineControlSpacing}"/>
            <Button Content="📂 Open Folder" Command="{Binding OpenBackupFolderCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"/>

            <!-- 12a: Progress spinner while running -->
            <TextBlock Text="⏳ Working…" FontSize="{StaticResource FontSizeBody}" FontWeight="SemiBold"
                       Foreground="{StaticResource FluentAccentDefault}"
                       VerticalAlignment="Center" Margin="{StaticResource FormColumnGap}"
                       Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>

        <!-- Messages -->
        <StackPanel Grid.Row="3" VerticalAlignment="Top" Margin="{StaticResource MessageBarSpacing}">
            <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ErrorMessageStyle}"/>
            <TextBlock Text="{Binding SuccessMessage}" Style="{StaticResource SuccessMessageStyle}"
                       h:AutoDismiss.IsEnabled="True" h:AutoDismiss.Seconds="4"/>
        </StackPanel>
    </Grid>
</UserControl>
